import 'package:flutter/material.dart';

class SweepCoinsWalletHeader extends StatelessWidget {
  const SweepCoinsWalletHeader({super.key, required this.wallet});

  final dynamic wallet; // Replace 'dynamic' with actual wallet type when known

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('SweepCoins Wallet Header - Placeholder'),
    );
  }
}
